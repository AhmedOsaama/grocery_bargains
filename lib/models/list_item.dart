class ListItem{
  String name;
  String description;
  String imageURL;
  int quantity;
  double price;
  bool isChecked;

  ListItem({required this.name,required this.price,required this.isChecked,required this.quantity,required this.imageURL,required this.description});
}